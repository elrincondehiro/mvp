---
// Button.astro
import type { CartItem } from '@/lib/cartStore';


interface Props {
  item: CartItem;
  // otros parámetros que necesites
}

const {item} = Astro.props;

---

<button class="add-to-cart-btn" id="addToCartButton" data-product-id={item.id} data-product-name={item.name} data-product-qty={item.quantity}>
  Agregar al carrito
</button>

<script>
  import { addToCart } from '@/lib/cartStore';

  const button = document.getElementById('addToCartButton');
  button?.addEventListener('click', () => {
    
    const productId = button.dataset.productId;
    addToCart({id:"hola", name:"producto", imageSrc:"http://www.google.es", price: 19.95, quantity:2});
    console.log("succces");
  });
</script>

<style>
  .add-to-cart-btn {
    padding: 0.5rem 1rem;
    background-color: #4CAF50;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }

  .add-to-cart-btn:hover {
    background-color: #45a049;
  }
</style>